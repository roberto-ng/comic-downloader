{
  "workspaces": {
    "packages": ["packages/*"],
    "nohoist": [
      "**/comic-downloader-electron/**"
    ]
  },
  "private": true,
  "scripts": {
    "build": "yarn workspace comic-downloader-core build",
    "start": "yarn workspace comic-downloader-electron electron-forge start",
    "package": "yarn workspace comic-downloader-electron electron-forge package",
    "make": "yarn workspace comic-downloader-electron electron-forge make"
  }
}
